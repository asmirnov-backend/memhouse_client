query GetBestMems($offset: Int = 0, $limit: Int = 3) {
  bestMems(GetMemsInput: { take: $limit, skip: $offset }) {
    id
    likes
    rating
    text
    images {
      title
      displayUrl
    }
  }
}
